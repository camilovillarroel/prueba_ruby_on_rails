<%= form_with(model: @todo, local: true) do |f| %>
 <%= f.label :description %>
 <%= f.text_field :description %>
 <%= f.check_box :completed %>
  <%= f.label :completed, 'Tarea completa' %>
 <%= f.submit 'Guardar' %>
<% end %>